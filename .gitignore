node_modules/
.cloudflare/
*.log
.wrangler/

# Vite build output (deploy.yml rebuilds this; never track it)
client/dist/

# Claude Code tooling (per-worktree, not committed)
.claude/

# Generated PDFs that shouldn't be tracked in source
client/RMPG-Dispatch-Guide-*.pdf

# Per-session scratch from Utah statute scraping work (XML downloads,
# generated SQL, parser scripts). Lives in worktrees only; never ship.
.firecrawl/
